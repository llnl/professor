# Analytical

Compare model solutions with exact solutions if they are calculatable.

---

This [napari] plugin was generated with [Cookiecutter] using [@napari]'s [cookiecutter-napari-plugin] template.

## Installation

You can install `analytical` via [pip]:

    pip install -e ./plugins/analytical

